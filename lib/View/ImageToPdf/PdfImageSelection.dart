import 'package:flutter/material.dart';

class PdfImgeSelection extends StatelessWidget {
  const PdfImgeSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
